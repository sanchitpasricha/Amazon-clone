import React from "react";

function Header() {
  return (
    <div>
      {/* logo on the left of the header*/}
      {/* search bar*/}
      {/* 3 links*/}
      {/* Basket icon with number */}
    </div>
  );
}

export default Header;
